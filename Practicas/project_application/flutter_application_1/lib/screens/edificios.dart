import 'package:flutter/material.dart';

class Edificios extends StatelessWidget {
  const Edificios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edificios'),
    );
  }
}
